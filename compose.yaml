services:
  reporter:
    build: .
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    volumes:
      - reporter-state:/app/state

volumes:
  reporter-state:
